// lets create sum and different function here and the export them so that any file can use them

export function sum(a = 0, b = 0) {
  console.log(`Sum of a and b is ${a + b}`);
}
export function difference(a = 0, b = 0) {
  console.log(`Diffrence of a and b is ${a - b}`);
}
